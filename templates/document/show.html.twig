{% extends 'base_modern.html.twig' %}

{% block title %}Détails du document{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header">
                    <h2>📖 Détails du document</h2>
                </div>
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <h3 class="card-title">{{ document.titre }}</h3>
                        <span class="badge {% if document.disponible %}bg-success{% else %}bg-danger{% endif %}">
                            {{ document.disponible ? 'Disponible' : 'Emprunté' }}
                        </span>
                    </div>
                    
                    <h5 class="text-muted mb-4"><i class="fas fa-user-edit"></i> {{ document.auteur }}</h5>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <strong>Type :</strong> {{ document.type }}
                        </div>
                        <div class="col-md-6">
                            <strong>ISBN :</strong> {{ document.isbn ?? 'Non renseigné' }}
                        </div>
                    </div>

                    <div class="mb-3">
                        <strong>Date d'acquisition :</strong> {{ document.dateAcquisition ? document.dateAcquisition|date('d/m/Y') : '-' }}
                    </div>

                    <hr>

                    <h4>Résumé</h4>
                    <p class="card-text">
                        {{ document.resume ?? 'Aucun résumé disponible.' }}
                    </p>

                </div>
                <div class="card-footer d-flex justify-content-between">
                    <a href="{{ path('document_index') }}" class="btn btn-secondary">
                        ← Retour à la liste
                    </a>
                    <div>
                        <a href="{{ path('document_edit', {'id': document.id}) }}" class="btn btn-warning">
                            <i class="fas fa-edit"></i> Éditer
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
