{% extends 'base_modern.html.twig' %}

{% block title %}{{ adherent.prenom }} {{ adherent.nom }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header">
                    <h2>👤 {{ adherent.prenom }} {{ adherent.nom }}</h2>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <strong>Email :</strong> {{ adherent.email }}
                    </div>
                    <div class="mb-3">
                        <strong>Téléphone :</strong> {{ adherent.telephone ?? 'Non renseigné' }}
                    </div>
                    <div class="mb-3">
                        <strong>Adresse :</strong> {{ adherent.adresse ?? 'Non renseignée' }}
                    </div>
                    <div class="mb-3">
                        <strong>Date d'inscription :</strong> {{ adherent.dateInscription|date('d/m/Y') }}
                    </div>
                    <div class="mb-3">
                        <strong>Statut :</strong>
                        {% if adherent.actif %}
                            <span class="badge bg-success">Actif</span>
                        {% else %}
                            <span class="badge bg-secondary">Inactif</span>
                        {% endif %}
                    </div>

                    <hr>

                    <h4>Emprunts en cours</h4>
                    {% set empruntsEnCours = adherent.emprunts|filter(e => e.dateRetourEffective is null) %}
                    {% if empruntsEnCours|length > 0 %}
                    <ul class="list-group">
                        {% for emprunt in empruntsEnCours %}
                        <li class="list-group-item">
                            <strong>{{ emprunt.document.titre }}</strong><br>
                            Emprunté le {{ emprunt.dateEmprunt|date('d/m/Y') }}<br>
                            Retour prévu le {{ emprunt.dateRetourPrevue|date('d/m/Y') }}
                            {% if emprunt.statut == 'en_retard' %}
                                <span class="badge bg-danger">En retard</span>
                            {% endif %}
                        </li>
                        {% endfor %}
                    </ul>
                    {% else %}
                    <p class="text-muted">Aucun emprunt en cours.</p>
                    {% endif %}
                </div>
                <div class="card-footer">
                    <a href="{{ path('adherent_index') }}" class="btn btn-secondary">
                        ← Retour à la liste
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}